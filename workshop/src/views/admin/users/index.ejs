<div class="container">
  <div class="row align-items-center">
    <div class="col">
      <h1>Usuarios</h1>
    </div>
    <div class="col text-end">
      <a href="/admin/users/create" class="btn btn-sm btn-primary">Crear</a>
    </div>
  </div>

  <table class="table table-striped">
    <thead>
      <tr>
        <th>#</th>
        <th>Correo</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% if (typeof rows == 'object') { %> <% rows.forEach(user => { %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.email %></td>
        <td class="text-end">
          <a href="/admin/users/edit/<%= user.id %>" class="btn btn-sm btn-info"
            >Editar</a
          >
          <form
            class="form-delete"
            action="/admin/users/<%= user.id %>?_method=DELETE"
            method="post"
            style="display: inline-block"
          >
            <button type="submit" class="btn btn-sm btn-danger">Borrar</button>
          </form>
          <a
            href="/admin/users/roles/<%= user.id %>"
            class="btn btn-sm btn-info"
            >Roles</a
          >
        </td>
      </tr>
      <% }) %> <% } %>
    </tbody>
  </table>
</div>

<script>
  document.addEventListener("submit", (event) => {
    if (event.target.classList.contains("form-delete")) {
      event.preventDefault();

      if (confirm("¿Esta seguro?")) {
        event.target.submit();
      }
    }
  });
</script>
